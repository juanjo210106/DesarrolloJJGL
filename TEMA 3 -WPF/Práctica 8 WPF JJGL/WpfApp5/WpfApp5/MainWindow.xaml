<Window x:Class="WpfApp5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Command="ApplicationCommands.Cut" 
                        CommandTarget="{Binding ElementName=txtEditor}" 
                        ToolTip="Cortar (Ctrl+X)">
                    <Image Source="/cortar.png" Width="16" Height="16" />
                </Button>

                <Button Command="ApplicationCommands.Copy" 
                        CommandTarget="{Binding ElementName=txtEditor}" 
                        ToolTip="Copiar (Ctrl+C)">
                    <Image Source="/copiar.png" Width="16" Height="16" />
                </Button>

                <Button Command="ApplicationCommands.Paste" 
                        CommandTarget="{Binding ElementName=txtEditor}" 
                        ToolTip="Pegar (Ctrl+V)">
                    <Image Source="/pegar.png" Width="16" Height="16" />
                </Button>

                <Separator />

                <Button Click="Mostrar_Click" 
                        ToolTip="Mostrar texto seleccionado">
                    <Image Source="/ojo.png" Width="16" Height="16" />
                </Button>
            </ToolBar>
        </ToolBarTray>

        <TextBox 
            Name="txtEditor" 
            AcceptsReturn="True" 
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto"
            FontSize="14"
            Text="Selecciona texto aquí y usa la barra de herramientas."/>

    </DockPanel>
</Window>
