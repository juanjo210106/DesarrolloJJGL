<Window x:Class="WpfApp4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Open" CanExecute="OpenCommand_CanExecute" Executed="OpenCommand_Executed"/>
        <CommandBinding Command="ApplicationCommands.Save" CanExecute="SaveCommand_CanExecute" Executed="SaveCommand_Executed"/>
    </Window.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="center" Margin="5">
            <Button Command="ApplicationCommands.Open" Name="btnAbrir" Margin="5" Padding="10,5" Click="btnAbrir_Click" ToolTip="Este botón permite abrir un fichero" >Abrir fichero</Button>

            <Button Command="ApplicationCommands.Save" Name="btnGuardar" Margin="5" Padding="10,5" Click="btnGuardar_Click" ToolTip="Nos permite guardar en un fichero">Guardar fichero</Button>
        </StackPanel>
        <TextBox Name="txtContenido" Grid.Row="1" Margin="10" TextWrapping="Wrap" AcceptsReturn="True" Height="300" FontSize="14"/>
    </Grid>
</Window>
