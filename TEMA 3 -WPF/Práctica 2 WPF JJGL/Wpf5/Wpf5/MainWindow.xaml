<Window x:Class="Wpf5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf5"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <!-- TextBox donde se escribe -->
            <TextBox Name="TextBoxEntrada"
                 Height="100"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 SelectionChanged="TextBoxEntrada_SelectionChanged"
                 FontSize="16"/>

            <!-- TextBox que muestra el texto seleccionado -->
            <TextBox Name="TextBoxSeleccion"
                 Height="30"
                 Margin="0,10,0,0"
                 IsReadOnly="True"
                 FontSize="16"
                 >El texto que has seleccionado es:</TextBox>
        </StackPanel>
    </Grid>
</Window>
