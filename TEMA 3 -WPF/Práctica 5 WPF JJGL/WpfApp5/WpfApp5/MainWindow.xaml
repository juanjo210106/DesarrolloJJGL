<Window x:Class="WpfApp5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="Ventana tipo Menú con DockPanel" Height="300" Width="500">

    <DockPanel>

        <StackPanel DockPanel.Dock="Top" 
                    Orientation="Horizontal" 
                    Background="#ECECEC">
            <Button Content="Archivo" Margin="5" Padding="10,5"/>
            <Button Content="Editar" Margin="5" Padding="10,5"/>
            <Button Content="Ayuda" Margin="5" Padding="10,5"/>
        </StackPanel>

        <TextBlock DockPanel.Dock="Bottom" 
                   Text="Terminado" 
                   Background="#01488e" 
                   Foreground="White" 
                   Padding="10,5" 
                   TextAlignment="Center"/>

        <Grid Background="WhiteSmoke">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="Este es el contenido principal en el Grid" 
                       FontSize="16" 
                       Margin="20" 
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"/>

            <Button Grid.Row="1" 
                    Content="Hacer algo" 
                    Width="120" 
                    Height="30" 
                    Margin="20"
                    HorizontalAlignment="Center"/>
        </Grid>

    </DockPanel>
</Window>
